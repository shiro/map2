let foo = true;
print(foo);

d::m;

{btn_forward down}::"hello";

a::{
  print(foo);
};


on_window_change(||{
  print(foo);
  a::a;
  if(active_window_class() == "Alacritty"){
    a::d;
  }
});

foo = false;
